{
  "name": "dtx-download-manager-gui",
  "version": "1.0.0",
  "description": "DTX Download Manager Frontend",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "build:watch": "tsc --watch",
    "copy-assets": "cp *.html *.css dist/",
    "dev": "npm run build:watch",
    "serve": "python3 -m http.server 8080 --directory dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {}
}
